/** sahftrjhesj ew**
**/
INT f(INT x, INT y ) 
BEGIN
    z :=x; 
    z := x*x - y*y;
    RETURN x+z;
END 

INT MAIN f1() 
BEGIN
    REAL x;
    READ(x, "A41.input");
    REAL y;
    READ(y, "A42.input");
    INT z;

    z:=0; 
    IF (x!=y) z := f2(x,y) + f2(y,x); 
    WRITE (z, "A4.output"); 
 END 
